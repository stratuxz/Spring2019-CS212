In main.py:

	The createDB() is set to “DROP” and “CREATE” the Dictionary table each time.
	
	Therefore, if you don’t want to create the table at each run and you want the user’s 
	typed in word to permanently store in the DB, then comment out createDB(). 
